pipeline {
  agent any
  stages {
    stage('prepare') {
      steps {
        deleteDir()
      }
    }
    stage('build') {
      steps {
        sh 'echo "Hi muje nind nahi aarahi "'
      }
    }
  }
}